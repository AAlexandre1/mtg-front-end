import { FilterCardsBySetPipe } from './filter-cards-by-set.pipe';

describe('FilterCardsBySetPipe', () => {
  it('create an instance', () => {
    const pipe = new FilterCardsBySetPipe();
    expect(pipe).toBeTruthy();
  });
});
